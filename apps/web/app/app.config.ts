export default defineAppConfig({
  ui: {
    // Рампи з app/assets/css/main.css — обидві побудовані на сімці кольорів
    // дизайн-системи Marmúr (espresso — акцент, sand — нейтраль).
    colors: {
      primary: "espresso",
      neutral: "sand",
    },
  },
});
